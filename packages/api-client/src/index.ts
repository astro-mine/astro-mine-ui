// @astro-mine/api-client — the generated client for astro-mine-api.
//
// Deliberately empty. The skeleton exists so the workspace, the build graph and the layering gate
// are real before any of them is under pressure; ui#2 generates the client from the API's OpenAPI
// document and adds the drift gate that fails CI when the committed output and the live document
// disagree.
//
// This package is a leaf: it must not import any sibling.

export {};
